name = "MatchingEngine"
version = "0.1.0"
defaultTargets = ["MatchingEngine", "matchingengine"]

[[lean_lib]]
name = "MatchingEngine"

[[lean_exe]]
name = "matchingengine"
root = "Main"

# Cross-artifact well-formedness differential (see scripts/wf_differential.sh).
# Deliberately not in defaultTargets: built on demand, not on every `lake build`.
[[lean_exe]]
name = "wfemit"
root = "WFEmit"
